# creating priority c;ass manifext and applying pod scheduling terms and then referencing them back in pod spc:
apoiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: high-priority
  namespace: default
values: 1000000000
flobalDefault: false
description: priority class should be used for this pod class.