apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
    tier: poc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nginx # should match pod selector
  ports:
  - name: http
    port: 80 # service port
    targetPort: 80 # container port
    protocol: TCP